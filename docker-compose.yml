version: "3.8"

services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    restart: unless-stopped
  client:
    build:
      context: ./client/web-ip-intelligence
    ports:
      - "4000:80"
    restart: unless-stopped
    depends_on:
      - server
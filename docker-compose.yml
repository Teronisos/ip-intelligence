version: "3.8"
services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    restart: unless-stopped

  client:
    build:
      context: ./client/web-ip-intelligence
    ports:
      - "4000:80"
    depends_on:
      - server
    restart: unless-stopped

